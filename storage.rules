rules_version = '2';
// slimemytaste uses Firestore only; canvas is HTML in Firestore, not Storage.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
